// src/components/StatisticsView.js

// 기존 함수들
export const getDailyRange = (today) => {
    // 함수 내용...
};

export const getWeeklyRange = (today) => {
    // 함수 내용...
};

export const getMonthlyRange = (today) => {
    // 함수 내용...
};

// 또는 모든 함수를 한 번에 export
export { getDailyRange, getWeeklyRange, getMonthlyRange };
